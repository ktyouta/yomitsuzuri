---
name: feature-resume
description: |
  別日・別セッションで作業を再開するとき、git と設計書から現在の進捗を判断し、
  feature-impl のどのステップから再開すべきかを報告する。

  以下の条件を満たす場合に必ず呼び出す：
  - 実装を伴う依頼を受けたとき、feature-impl を呼び出す前に docs/ 配下に対象機能の plan.md が存在するか確認し、存在する場合は必ずこのスキルを先に実行する
  - 「再開」「続き」「前回」など継続を示す言葉がある場合も同様
  （特定のフレーズではなく plan.md の存在を主な判断基準とする）

  以下の場合は使用しない：
  - docs/ 配下に対象機能の plan.md が存在しない新規実装（→ feature-impl を使う）
  - 調査・説明のみの場合
version: 1.0.0
---

# Feature Resume Skill

## Overview

git の状態と設計書を読み取り、前回の作業がどこまで完了しているかを判断して報告する。
feature-impl の途中で止まった作業を、正しいステップから再開することが目的。

---

## Procedure

### Step 1: git の状態を確認する

以下を実行する：

- `git log --oneline -10` — 直近のコミット履歴を確認
- `git status` — 未コミットの変更を確認
- `git diff --stat` — 変更ファイルの概要を確認

---

### Step 2: 設計書を確認する

`docs/` 配下の `plan.md` を確認し、何の機能を実装しようとしていたかを把握する。

---

### Step 3: 進捗を判断する

Step 1・2 の情報をもとに、feature-impl のどのステップまで完了しているかを判断する。

| 状態 | 判断基準 |
|---|---|
| Step 3 未完 | plan.md はあるが、バックエンドのコミットがない |
| Step 4〜6 完了 | バックエンドのコミットがある、フロントエンドのコミットがない |
| Step 7〜9 完了 | フロントエンドのコミットもある、spec-review 未実施 |
| 完了 | 全ステップのコミットがあり、未コミット変更がない |

---

### Step 4: 結果を報告する

```
## 作業再開レポート

### 実装中の機能
- [機能名]: docs/[機能名]/plan.md より

### 完了済みステップ
- [完了しているステップを列挙]

### 再開ポイント
- feature-impl の Step [N] から再開してください
- 理由: [git log / git status から読み取った根拠]

### 未コミットの変更
- [あれば列挙、なければ「なし」]
```

---

## Constraints

- git コマンドの実行結果を根拠として示す（推測で判断しない）
- 判断が難しい場合はユーザーに確認する
- 実装は行わない（再開ポイントの報告のみ）
