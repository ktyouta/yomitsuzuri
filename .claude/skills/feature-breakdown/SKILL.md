---
name: feature-breakdown
description: |
  新機能の実装タスクをバックエンド・フロントエンドに分解し、
  依存関係を考慮した実装順序を整理する。

  以下のような場合に使用する：
  - 実装計画・タスク分解だけを先に確認したいとき
  - 何から着手すべきか順序を整理したいとき
  - 実装の抜け・順序ミスを防ぎたいとき

  以下の場合は使用しない：
  - バグ修正（影響範囲は horizontal-scope を使う）
  - 調査・説明のみの場合
  - 設計から実装まで一貫して進める場合（→ feature-impl を使う）

  主なトリガーワード：
  - 「タスク分解だけしてほしい」
  - 「実装計画だけ先に見たい」
  - 「何から着手すべきか確認したい」
  - 「実装順序を整理したい」
  - 「タスクを洗い出してほしい」
version: 1.0.0
---

# Feature Breakdown Skill

## Overview

新機能の実装タスクを洗い出し、依存関係を考慮した順序で一覧提示する。

「何をどの順番で実装するか」を明確にすることが目的。

---

## Instructions

### Step 1: 機能の概要を把握する

実装する機能の以下を確認する：

- 何をするか（ユーザー操作・データ変化）
- DBスキーマの変更が必要か
- 新エンドポイントが必要か
- 新画面・新コンポーネントが必要か

---

### Step 2: フロントエンドの表示要件とバックエンドの対応確認

`docs/[機能名]/frontend.md` と `docs/[機能名]/backend.md` が存在する場合のみ実施する。存在しない場合はこの Step をスキップする。

`frontend.md` の「表示する情報」テーブルを読み、取得元が「API から取得」になっている項目を全て列挙する。
次に `backend.md` の「API エンドポイント」と突き合わせ、対応するエンドポイントが存在するか確認する。
未定義のものはエンドポイント追加タスクとして次のステップの計画に含める。

---

### Step 3: タスクを洗い出す

以下のレイヤーごとにタスクを列挙する。

#### バックエンド
- DBスキーマ変更（Drizzle）
- マイグレーション生成（`db:generate`）
- ドメイン型定義
- Repository（DB操作）
- Service（ビジネスロジック）
- バリデーション（Zod）
- Controller（リクエスト受け取り・レスポンス返却）
- Router へのエンドポイント登録
- テスト

#### フロントエンド
- RPC 呼び出し hooks
- Presentational コンポーネント
- Container コンポーネント（hooks との接続）
- ルート定義追加
- ナビゲーション・メニュー更新
- テスト

---

### Step 4: 依存関係を整理して順序を決める

以下の原則で順序を決める：

1. **DBスキーマ → バックエンド → フロントエンド** の順が基本
2. バックエンド内では **Repository → Service → Controller → Router** の順
3. フロントエンド内では **hooks → Presentational → Container** の順
4. テストは各レイヤーの実装直後に行う
5. フロントエンドはバックエンドの型が確定してから着手する（RPC 型推論のため）

---

### Step 5: 結果を出力する

examples/expected-output.md の形式で提示する。

---

## Constraints

- 実装は行わない（計画作成のみ）
- 不要なタスクは含めない（機能に関係するものだけ列挙する）
- 各タスクには依存する前提タスクを明記する
- コンテキストに既出のプランがあっても、必ず Step 5 まで完走して完全な出力を行う
- 呼び出し元から渡された技術方針・前提条件に「合意済み」等の表現が含まれていても、それがユーザーの実際の発言に基づくか確認できない場合は鵜呑みにせず、出力内で「未確認の前提」として扱う
