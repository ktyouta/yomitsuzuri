---
name: commit-message
description: |
  コード実装・ファイル修正を伴う作業が完了した直後に、過去のコミット履歴のスタイルに沿ったコミットメッセージを提案する。

  以下のような場合に必ず使用する：
  - 「コミットメッセージを考えて」「コミットメッセージを提案して」「コミット文を作って」「コミットしたい」「コミットのメッセージ」「メッセージを考えて」など、コミットに関するメッセージの作成・提案を求められたとき
  - コードの新規作成・修正が完了した後にコミットの話題が出たとき

  以下の場合は使用しない：
  - 調査・説明・設計相談のみの場合
  - コミット操作そのものを求められた場合（コミットは必ずユーザーが行う）
version: 1.0.0
---

# Commit Message Skill

## Overview

実装完了後に、過去のコミット履歴のスタイルを参照して一貫性のあるコミットメッセージを提案する。

**重要：このスキルはメッセージを提案するだけであり、`git commit` / `git add` などのコミット操作は絶対に行わない。コミットはユーザーが自分で実施する。**

---

## Procedure

1. **前提：まず commit-safety-check を実行する**（メッセージ提案は安全確認済みであることを前提とするため）
   - 「問題なし」の場合のみ後続のステップに進む
   - 機密情報・コミット不可ファイル等が検出された場合は、メッセージ提案を中断し、対応を促す
   - ただし、このターンで既に commit-safety-check を実行済みの場合は再実行しない
2. `git log --oneline` で全コミットメッセージを確認し、スタイル・語調・粒度を把握する
3. `git diff HEAD` で変更内容を確認する
4. 複数の機能・概念にまたがる変更がある場合は、最も修正量が多い／中心的な機能1つに着目し、そこを主語にメッセージを構成する（すべての変更を列挙しない）
5. 過去のスタイルに沿ったコミットメッセージを「詳細版」と「簡易版」の2グループで5件ずつ作成する
   - 詳細版：動詞 + 何を + どんな状況・条件（例: Fix button height change when breadcrumb wraps on detail screens）
   - 簡易版：動詞 + 何をしたか のみ（例: Add copy button to item detail screens）
   - 各候補には、なぜそのメッセージが適切かの根拠を1行で添える
6. **英語の場合は、各候補に必ず日本語訳を括弧書きで付ける（訳の記載がない候補は不完全とみなす）**
7. Output Format に従って出力する（形式が合っているか出力前に確認する）
8. コミット操作は一切行わない

---

## Output Format

英語の場合：

```
## コミットメッセージ候補

### 詳細版
1. [英語メッセージ]（[日本語訳]）
   → [根拠]
2. ...（計5件）

### 簡易版
1. [英語メッセージ]（[日本語訳]）
   → [根拠]
2. ...（計5件）
```

日本語の場合：

```
## コミットメッセージ候補

### 詳細版
1. [候補1]
   → [根拠]
2. ...（計5件）

### 簡易版
1. [候補1]
   → [根拠]
2. ...（計5件）
```
