---
name: design-proposal
description: |
  設計の相談・選択が必要なとき、複数案をメリット・デメリット付きで提示し、
  推奨案と「その案が破綻するケース」を合わせて提案する。

  以下のような場合に必ず使用する：
  - ユーザーがデザイン・UI・実装方針について「これで良いか」の判断を求めているとき
    （確認・相談・比較・懸念の表明など、表現の形は問わない）
  - 実装に入る前に選択肢を整理したいとき
  - 複数の実装アプローチで迷っているとき

  以下の場合は使用しない：
  - 設計が既に決まっていて実装に入るとき
  - 調査・説明のみの場合
version: 1.0.0
---

# Design Proposal Skill

## Overview

設計の選択が必要な場面で複数案を構造化して提示し、
根拠のある推奨案を選定する。

CLAUDE.md の設計指針（複数案・メリデメ・推奨・破綻ケース）を毎回確実に踏むことが目的。

---

## Instructions

### Step 1: 問題の本質を把握する

症状ではなく根本原因を理解する。

- 何を解決したいか
- 制約条件は何か（パフォーマンス・保守性・既存設計との整合性など）
- 変更の影響範囲はどこか

---

### Step 2: 複数案を洗い出す（最低2案・最大3案）

各案について以下を定義する：

- 概要（1〜2行）
- メリット
- デメリット
- 実装コスト（低・中・高）

---

### Step 3: 推奨案を1つ選定する

以下の基準で選ぶ：

- **現在の目的に対して最もシンプルか**
- **メンテナンス性が高いか**
- 将来の変更に対して柔軟か

選定理由を明記する。

---

### Step 4: 推奨案の破綻ケースを明示する

「将来どのような変更が入るとこの案が破綻するか」を必ず記述する。

これにより、将来の設計変更時に判断材料として使える。

---

### Step 5: 結果を出力する

examples/expected-output.md の形式で提示する。

---

## Constraints

- 実装は行わない（設計提案のみ）
- 推奨案は必ず1つに絞る（「どちらでも良い」は禁止）
- 破綻ケースを省略しない
- 現在の目的に不要な将来の考慮を過剰に盛り込まない
- 推奨理由・前提条件は Claude 自身の提案であり、ユーザーが承認するまでは「合意済み」と記載しない。対象範囲・要件が変わった後にこの提案を後続タスク（feature-breakdown 等）へ引き継ぐ場合は、前提が変更後も成立するか必ず再検証する
