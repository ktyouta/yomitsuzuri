---
name: commit-safety-check
description: |
  実装完了後またはコミットメッセージ依頼時に変更ファイルの内容を確認し、
  機密情報・コミットしてはいけないファイルが含まれていないかチェックする。

  以下のような場合に必ず呼び出す：
  - Claude による実装・ファイル修正が完了したとき
  - 「コミットメッセージを考えて」など、コミットメッセージの作成を求められたとき
  - 「コミット前チェックして」「機密情報が入ってないか確認して」と依頼されたとき

  以下の場合は使用しない：
  - 変更ファイルがない場合
  - 調査・説明・設計相談のみの場合
version: 1.0.0
---

# Pre-Commit Check Skill

## Overview

実装完了後またはコミットメッセージ依頼時に、変更ファイルに機密情報が含まれていないかチェックする。

**重要：このスキルは機密チェックと報告のみを行い、`git commit` / `git add` 等の git 操作は一切行わない。**（後続スキルの実行はこの禁止対象に含まない）

---

## Check Instructions

### 機密ファイルの混入確認
以下のファイルが変更対象に含まれていないか確認する：
- `.env` / `.env.*` / `.dev.vars`
- `*.pem` / `*.key` / `*.p12` / `*.pfx`
- `*secret*` / `*credential*` という名前のファイル

### ハードコードされた機密情報
変更ファイルの差分（追加行）に以下のパターンがないか確認する：

| カテゴリ | 検出パターン例 |
|---|---|
| API キー | `sk-`、`ghp_`、`ghs_`、`AIza`、`AKIA` |
| パスワード | `password\s*=\s*["'][^"']+["']`、`passwd\s*=` |
| シークレット | `secret\s*=\s*["'][^"']+["']`、`SECRET\s*=` |
| 秘密鍵 | `-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----` |
| JWT | `eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+` |
| トークン | `token\s*=\s*["'][A-Za-z0-9+/]{20,}["']` |

### 手動マイグレーションファイルの混入確認

`git status` / `git diff HEAD --name-only` の結果に `drizzle/[0-9]*.sql` にマッチする新規ファイルが含まれている場合、`drizzle/meta/` 配下のファイル（`_journal.json` または `*_snapshot.json`）も同時に変更されているか確認する。

- `drizzle/meta/` に変更がない場合 → 手動作成の疑いあり（**コミット不可**）
- `drizzle/meta/` も変更されている場合 → `db:generate` 経由と判断（問題なし）

**なぜ禁止か**: 手動で SQL ファイルを作成するとスナップショットが更新されないため、以降の `db:generate` が誤った差分を計算する原因になる。

### ローカル環境依存パス（**絶対禁止**）
以下のパターンがコード・ドキュメントのいずれに含まれていても**コミット不可**とする：

| カテゴリ | 検出パターン例 |
|---|---|
| Windows 絶対パス | `C:\`、`D:\`、`C:/Users/` 等のドライブレター始まりのパス |
| Unix ホームディレクトリ | `/home/[username]/`、`/Users/[username]/` |

**なぜ禁止か**: ローカルパスはコードやドキュメントを他の環境で動かせなくする。チームで共有するファイルにはプロジェクトルートからの相対パスのみ使用すること。

---

## Procedure

1. `git diff HEAD --name-only` で変更ファイル一覧を確認する
2. 機密ファイル名が含まれていないか照合する
3. `git diff HEAD` で追加行の内容を確認し、以下を照合する
   - 機密情報パターン
   - ローカル環境依存パス（`C:\`、`D:\`、`/home/`、`/Users/` 等）
4. 新規追加ファイル（`git status` で `??` のもの）がある場合は、その内容も同様にチェックする
5. 以下の形式で報告する
6. 補足：この実行が「コミットメッセージ依頼」（例：「コミットメッセージを考えて」）を起点にしている場合は、報告後に commit-message を続けて実行する。機密チェックを単独で頼まれた場合（例：「機密情報が入ってないか確認して」）は続けない。

---

## Output Format

問題がある場合：

```
## Pre-Commit Check 結果

### 要確認
- **ファイル**: [ファイルパス:行番号]
- **検出内容**: 具体的な検出パターン
- **対応方針**: 環境変数化・ファイル除外等
```

問題がない場合：

```
## Pre-Commit Check 結果

チェック完了。問題なし。
```
