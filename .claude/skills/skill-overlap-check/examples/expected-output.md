# Expected Output — スキル重複チェック結果

---

## ケース1: 完全重複あり

### 入力
「実装後にテストファイルの漏れを確認するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 既存スキルで対応可能

`horizontal-gap-check` が同じ目的をカバーしています。

- **既存スキル**: `horizontal-gap-check`
- **重複内容**: 実装後に変更漏れ（テストファイル含む）を確認する
- **推奨**: 新規作成は不要。`horizontal-gap-check` のチェック対象にテストファイルの観点が不足している場合は improve-skills で拡張してください。
```

---

## ケース2: 部分重複あり（拡張推奨）

### 入力
「バックエンドの型定義漏れを確認するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 既存スキルへの追記を推奨

`rpc-review` が型安全性チェックを担っていますが、RPC 以外の型定義は対象外です。

- **既存スキル**: `rpc-review`
- **重複内容**: 型安全性の観点
- **差分**: `rpc-review` は RPC レスポンスのみ対象。バックエンド全体の型定義漏れは未カバー
- **推奨**: `rpc-review` に「RPC 以外の型アサーション検出」を追記するか、新規スキルとして分離するか検討してください。
```

---

## ケース3: 新規作成が妥当

### 入力
「設計案を複数提示して比較するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 新規作成を推奨

既存スキルに該当なし。新規作成してください。

- **既存スキルとの差分**: 設計案の提示・比較は現在どのスキルもカバーしていない
- **推奨スキル名**: `design-proposal`
- **次のステップ**: create-skills を使って作成してください。
```
